## Provisioning of a basic GCP Infrastructure using Terraform

This is just a simple terraform manifest for the provisioning of a VPC network with only one subnetwork and a compute instance in the network.